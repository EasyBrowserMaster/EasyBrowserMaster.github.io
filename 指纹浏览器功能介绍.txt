Chromium 指纹浏览器
真正的页签级隔离解决方案

副标题
为自动化 / 风控对抗 / 多账号环境打造的高隐匿浏览器

核心卖点（中部模块）

🧠 页签级指纹隔离

每个 Tab 独立指纹环境
语言 / 时区 / 分辨率完全隔离
WebGL / Canvas / Audio/WebRTC 指纹独立生成

🛡 深度指纹防护

Canvas 噪声混淆
WebGL 指纹重写
AudioContext 指纹伪装
完整 Worker 指纹处理（含 WebWorker / ServiceWorker）

🍪 Cookie 隔离机制

每标签独立 Cookie 空间
防关联、防串号
支持持久化与快速切换

🌐 代理隔离（核心能力）

Tab 级代理独立绑定
支持 HTTP / HTTPS / SOCKS5
动态切换不污染环境

⚙️ 高级 PAC 流量控制

精细化规则分流
按域名 / 路径 / 协议控制
避免指纹泄露与流量穿透

检测通过能力（高亮展示）

✅ browserscan 全绿
✅ creepjs 高分通过
✅ pixel 指纹检测绕过
✅ 主流指纹检测网站全兼容